export class AppSettings {
  public static BACKEND_SERVER = 'http://localhost';
  public static BACKEND_SERVER_PORT = 8080;
  public static BACKEND_SERVER_API = '';
  public static BACKEND_SERVER_URL = AppSettings.BACKEND_SERVER + ':' + AppSettings.BACKEND_SERVER_PORT + AppSettings.BACKEND_SERVER_API;
}
